# Delivery Agent

```
Usage of ./delivery:
  -redis_addr string
        address of redis server to listen for postback objects (default "127.0.0.1:6379")
  -redis_password string
        password for authenticating with redis (default "none")
```